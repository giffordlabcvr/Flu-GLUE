// get frequencies for every alignment columns

// get stop position in each sequence

  // if stop occurs before end of constraining reference, adjust counts 
  
    // subtract 1 from all counts downstream if stop
    
    